node_modules/
dist/
.DS_Store
*.local
.vite/

.vercel

# Firebase-Konfiguration (daten/cloud.ts) — keine Geheimnisse (Firebase-Web-
# Konfigurationswerte sind oeffentlich einsehbar), aber projekteigen und
# nicht Teil des Repos. Siehe .env.example fuer die erwartete Form.
.env
