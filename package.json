{
  "name": "bruehwerk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ein Laborbuch für Kaffee — Shot-Logging, Dial-in, Bestellung in Durchgängen",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:unit": "vitest run",
    "test:typen": "svelte-check --tsconfig ./tsconfig.json --threshold error --output human",
    "test": "npm run test:unit && npm run test:typen && npm run build"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@tsconfig/svelte": "^5.0.8",
    "@types/node": "^26.2.0",
    "svelte": "^5.56.9",
    "svelte-check": "^4.7.6",
    "typescript": "^6.0.3",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "idb": "^8.0.3",
    "zod": "^4.4.3"
  }
}
